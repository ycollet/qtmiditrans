Remove 2 of the 3 apply buttons in each tab. One is enough.
Find a better value for skipAccel (maybe use a frame value).
Add a small pdf doc.
Add in the doc how to compile the package.
Add install rules.

